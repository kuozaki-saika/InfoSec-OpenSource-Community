# 安全审查清单

## 输入验证

- [x] 文件扩展名白名单校验（png/jpg/jpeg/bmp/tiff/tif）
- [x] 空文件名检测
- [x] 文件大小上限（16MB）
- [x] `secure_filename` 清理文件名
- [x] UUID 前缀防冲突和路径猜测

## 认证授权

- [x] 频率限制（每 IP 20次/分钟）作为基本访问控制
- [N/A] 用户认证 — 当前为内部工具，暂不需要

## 错误处理

- [x] 统一 JSON 错误格式，不暴露内部路径
- [x] 413 错误返回友好提示
- [x] 图像解析失败返回 422 而非 500
- [x] 异常兜底返回通用错误信息

## 数据安全

- [x] 处理完成后立即删除上传的临时文件
- [x] 输出文件名去敏
- [x] 模型使用 pickle 加载但仅加载本地受信文件

## 运行环境

- [x] `debug=False`（生产模式）
- [x] 绑定 127.0.0.1，不对外暴露
- [x] Flask 内置服务器仅用于开发/演示

## 审查结论
所有适用项已通过，无未处置的高危风险。
