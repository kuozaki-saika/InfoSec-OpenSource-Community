# 提示词记录 — 靶场认证系统

工具：Claude Code

## 第1轮：需求理解

**提示词：**
> 成员1（林翔，组长）— 核心认证系统：用户注册/登录/登出、Session 会话管理、密码 bcrypt 哈希存储

**AI 行为：** 分析现有 upload-labs 代码结构（PHP，无数据库），确定技术方案。

## 第2轮：代码生成（含约束）

**隐含约束（来源：约束文档）：** bcrypt 密码存储、session_regenerate_id 防会话固定、htmlspecialchars 防 XSS、输入白名单校验

**AI 生成文件：**
-  — 核心认证逻辑（会话管理、用户CRUD、bcrypt 哈希）
-  — 登录页面 + 处理逻辑
-  — 注册页面 + 输入校验
-  — 登出处理

## 第3轮：改造集成

**提示词：** 修改 config.php 和 head.php 集成认证系统

**AI 行为：**
- config.php 新增 
- head.php 新增未登录重定向 + 用户名显示 + 退出按钮

## 第4轮：审查修正

**发现问题：** logout.php 未调用 

**修正：** 改为  完整清理
